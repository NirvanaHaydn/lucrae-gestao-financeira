=======================================================================================================================
ARQUIVOS:

global.css --> Estilos globais usados em toda a aplicação, como:
                cores principais
                tipografia
                layout base
                resets de estilo.

dashboard.css --> Estilos específicos para o dashboard financeiro.

forms.css --> Estilos para formulários da aplicação, como:
                login
                registro
                formulários de receita e despesa.


=======================================================================================================================